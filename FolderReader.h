#pragma once
#include <string>
#include <vector>
using namespace std;

class FolderReader
{

public:
	FolderReader(string, string, string, string, int);
	FolderReader(string, string, string);
	~FolderReader();
};

